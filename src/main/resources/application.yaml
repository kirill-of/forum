spring:
  profiles.active: production
  application:
    name: myforum
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  messages:
    basename: messages
---
spring:
  profiles: dev
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/myforum
    username: root
logging:
  file: logs/log_dev.txt
server:
  port: 8090
---
spring:
  profiles: production
  jpa:
    hibernate:
      ddl-auto: create-drop
  database-platform : org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    driver-class-name : com.mysql.jdbc.Driver
    url: jdbc:mysql://eu-cdbr-west-02.cleardb.net/heroku_af0b266690d15bd?reconnect=true
    username: b206f97360abee
    password: e7d860af